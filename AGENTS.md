# AGENTS.md — Swarm グローバル記憶 (kpango/dotfiles)

エージェントの成功・失敗の軌跡を記録し、破滅的忘却と同一エラーの無限ループを防ぐ。
運用ルールは `claude/SWARM.md` §5 を参照。

## 運用

- 修正ループに入る**前に**このファイルと `@fix_plan.md` を読む。
- タスク完了・予算超過・エスカレーション時に 1 行追記する（人間向けの散文にしない）。
- 同一エラーへ同一対処を 2 回連続で行わない。過去の軌跡に同じ症状があれば「学び」欄の対処から始める。

## 軌跡

| 日付 | タスク | 試行 | 結果 | 学び（根本原因・有効だった手順） |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-13 | swarm 制御基盤の初期構築 | 1 | success | SWARM.md + hooks + skills 一式を導入。`~/.claude/skills`・`~/.claude/hooks` は dotfiles への symlink のため配置即反映 |
| 2026-07-13 | swarm-loop (Massive Agents Loop) 追加 | 1 | success | 自走ループの唯一の入口を /swarm-loop に統一。進行状態は @fix_plan.md が SSOT、mission-init.sh は重複起動を拒否、loop-status.sh でセッション跨ぎ再開。hadolint 導入は人間判断待ちでスキップ |
| 2026-07-13 | Deep Research による swarm skill 群の洗練 | 2（Fable レート制限・セッション上限で 2 回に分割） | success | MAST 失敗分類(3カテゴリ14モード)導入・intra-family(Sonnet/Opus)verifier の理論的限界を明記し決定論的ツールを第一権威に格上げ・debate によるバイアス増幅を根拠に Maker/Checker 討論を禁止・5試行中3試行目のソフトチェックポイント追加(Repair@N が3試行目で頭打ちという一次データに基づく)・学びの3段階モデル(点修正→明文化→機械化)を AGENTS.md 運用に追加・GATE で「全チェックgreen≠安全」を明記(自動監査は事前予防0%・事後回帰防止87%という知見)。SWARM.md §1 以外の章番号を全面採番し直したため他ファイルからの参照は §1 のみに限定した |
| 2026-07-13 | 全 Skills のリファクタ・統廃合 | 1 | success | 最大の重複は dig(既存・成熟)と swarm-loop(新規)。dig は単一セッション・対話的設計インタビュー向け、swarm-loop は100体探索・複数セッション永続化を要する大規模自律ミッション向けと役割分担を明文化し両者を相互参照させた。dig の Code Quality Reviewer が Implementer と同じ sonnet だった intra-family 問題を発見し opusplan に格上げ。逆に dig の Circuit Breaker (Transient/Permanent エラー分類・失敗シグネチャ2回一致での早期検知・強制内省テンプレート) を swarm-implement の Fixer トリガーに逆輸入し、機械的な「3試行目固定」から「3試行目 or Permanent エラー or シグネチャ一致のいずれか早い方」に改善。security-review/security-scan/security-bounty-hunter・github-actions-patterns/deployment-patterns は対象(アプリコード/Claude設定/バウンティハンティング、Actions YAML/デプロイ戦略)が異なり統合不要と判断し据え置き。golang-patterns 等8件の大型パターン/テストskill(400-800行)はProgressive Disclosure未適用だが、議論の主題(ループオーケストレーション)と直接関係が薄く今回はスコープ外とした |
| 2026-07-13 | dig を swarm-loop へ完全統合（人間の使い分けコスト排除） | 1 | success | 前回は「相互参照する2skill」で留めたが、人間が毎回dig/swarm-loopを選ぶコスト自体が問題との指摘を受け完全統合。swarm-loopにPhase -1(SCALE判定: Quick/Interactive/Mission、昇格のみ許可)を新設し、dig独自だった対話的設計インタビュー(選択肢提示・優先度順質問)をPLANフェーズへ、TDAD Iron Law+複雑度ガード(trivial/simple/standard/complex)をswarm-implementへ統合。task-template.mdはdig/からswarm-implement/へ移動、Model行をsonnet/opus固定ポリシーに合わせ修正。dig/SKILL.mdは`Skill(swarm-loop, args=...)`を呼ぶだけの薄いリダイレクトに縮小(後方互換のため/digトリガーは維持)。mission-init.shに`scale`引数を追加しloop-status.shで表示。並列上限はdigの5ではなくswarm既存の3(Checkerレビュー品質の実証根拠あり)を採用 |
| 2026-07-13 | swarm-evolve（人間承認必須のメタ進化ループ）新設 | 1 | success | 「対話でSKILL.mdを自己更新できるか」との質問に対し、現状は人間が気づいて手動編集する形でしか機能していなかった(このセッション全体がその実例)ため、機構化に着手。Drafter(sonnet)が AGENTS.md+evolve-log.jsonl(hookのrejectionを記録する新設ログ)から2回以上の繰り返しパターンのみを起案 → Checker(opus、Drafterの理由づけは見せず証拠とdiffのみで判定)が誤分類(behavioralをdocs-onlyに偽装)を反証的にチェック → **人間承認は docs-only でも省略不可**(ユーザーの明示要望どおり、自動適用ティアは作らなかった)。hookにlog_evolve_event()を追加しswarm-post-edit-lint.sh/swarm-stop-verify.shの各rejectionを`~/.claude/session-data/swarm/evolve-log.jsonl`に記録。`/loop 1d /swarm-evolve`で定期起動しても承認フェーズはスキップされない設計 |
| 2026-07-13 | swarm系skill群の一般レビュー（人間の直接依頼、swarm-evolve経由ではない） | 1 | success | 「Skillを改善して」という曖昧な依頼に対しAskUserQuestionで対象を絞り込み(swarm-evolveの証拠駆動/Progressive Disclosure/一般レビュー/個別課題の4択)、一般レビューを選択。全swarm系SKILL.md+付属scriptsを通読し4件の整合性ギャップを発見・修正: (1) swarm-explore の Workflow 秘書プロンプトが swarm-secretary/SKILL.md の契約(MAST分類・Priority Queue表・Root Causes・Unverified形式)を要求しておらず下流で情報が欠落 → プロンプトに契約を明示的に埋め込み。(2) task-template.md の Output Schema(DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED)と@fix_plan.mdのstatus語彙(done/blocked(budget)/blocked(design)/blocked(spec))の対応関係が未定義 → 変換表を追加。(3) swarm-loop GATE Option4(破棄)でworktree-release.shに--delete-branchを付ける指示がなく`swarm/*`ブランチが永久に残る → 明記。(4) swarm-secretaryが付与するmast分類がTasksテーブル変換時に列がなく失われる → note列への転記を指示。いずれも既存の状態機械・閾値・禁止事項は変更せず、層間の情報伝達契約を補完するdocs-only相当の修正。今回はswarm-evolveのDrafter/Checker/人間承認フローを経由していない(人間による直接の一回性レビュー依頼であり、AGENTS.md軌跡からの繰り返しパターン検出ではないため) — 今後同種の「Skillを改善して」という曖昧な依頼が2回目に来たら、対象選択質問自体をswarm-loop Phase -1のSCALE判定に機械化するかどうか検討する |
| 2026-07-13 | swarm-evolve: DeepResearch起点のSWARM.md §8改訂（Agent Teams見送り訂正・MAV/Entropy Principle論文追加） | 1 | success | 通常トリガー(AGENTS.md/rejectionログの2回反復)ではなくDeepResearchの外部一次資料3件を証拠として例外的に起案(Drafterが冒頭で逸脱を明示)。Checker(Opus、独立コンテキスト)が反証的に検証: 証拠2(MAV, arXiv:2502.20379)・証拠3(Entropy Principle, arXiv:2606.08162)は§8「一次ソースのみ」区分への参考文献追加としてapproved。証拠1(Agent Teams実在確認)は初稿で「DeepResearchのadversarial検証で確認済み」という証拠に無い過大主張を含んでおりCheckerがfalse判定→「primary一次資料で実在確認、adversarial検証は未完了」に修正させ再提出。3件とも人間が個別承認し、docs-onlyとして適用(§2〜§7のbehavioralな規定は無変更、Agent Teamsの見送り決定自体も維持)。学び: 外部文献ベースの起案でも「adversarial検証済み」と「一次ソースのみ」の混同はCheckerが機械的に検出可能 — swarm-evolveのCheckerステップは通常の反復パターン検証だけでなく、確度階層の内部整合性チェックとしても機能する |
| 2026-07-13 | skill-effectiveness-audit: 「全Skillが効果的にフル性能を発揮できるよう設定とルールを見直し・修正・リファクタリング」ミッション(Mission scale) | 1 | success | swarm-explore(Haiku 16シャード、全28 SKILL.md+settings.json×2+hooks17件+agents9件)→秘書集約(124→61件)。PLAN段階の「自動解決」で秘書レポートを鵜呑みにせず一次ファイルで裏取りした結果、61件中55件は誤検知と判明(settings.jsonのグローバル/プロジェクト層「hook欠落」6件→vald repoにも正しく配線済みで意図的階層設計、vald-law2-gate.shのf-string注入疑惑→$ALTはsys.argv[1]経由で安全、agent model=sonnet→CLAUDE.md自身が意図的指定、rtk-rewrite.shの無音exit→コメント明記の意図的挙動、settings.local.jsonのSkill(*)許可→disable-model-invocationという独立ガードで無害と本セッション内で実証済み)。確定した実バグのみ修正: benchmark/SKILL.mdの別テンプレート("ECC")由来の壊れた参照除去、settings.json PostToolUse matcherへのMultiEdit追加、SWARM.mdのopusplan表記訂正。EXECUTE中にHaiku指摘(hooksへの`-e`追加)を一度採用しかけたが、`out=$(cmd);rc=$?`パターンを`-e`が壊し検証ゲート自体を破壊すると実装前に検証で発覚し見送り(security-gate.shのfail-open補強も同様にROI低いと判断し見送り)。code-reviewer(sonnet)による事後レビューでCLAUDE.mdのhooks表更新漏れ1件を検出・修正。学び: Haiku群は「設定の階層構造」「意図的なfail-safe/fail-open挙動」「コメントに明記された設計意図」を読み取れず字面だけで誤検知しやすく、severity=critical/highは秘書レポートだけでなく必ず一次ファイルで裏取りしてから着手する運用が有効だった。**§5「2回目の学び機械化」トリガー**: AGENTS.md 2026-07-13の前回エントリ(「swarm系skill群の一般レビュー」)が「今後同種の『Skillを改善して』という曖昧な依頼が2回目に来たら対象選択質問をswarm-loop Phase -1のSCALE判定に機械化するか検討する」と予告しており、本ミッションはその2回目に相当する。機械化(swarm-loop Phase -1への組み込み)はSKILL.md自体の変更のため`/swarm-evolve`経由・人間承認が必要 — 次回`/swarm-evolve`実行時の入力候補として要検討（未着手）                                                                                                                                                                                                    |
| 2026-07-14 | settings-modernization: 「最新のClaude設定トレンド・推奨項目・マルチAgent機構をリサーチしsettings.json等を改善」ミッション(Mission scale)           | 1                                                  | success | DeepResearchで2026年のClaude Code設定ベストプラクティスを調査。結果、現行`claude/settings.json`は既にautoMemoryEnabled/autoMemoryDirectory/cleanupPeriodDays/fileCheckpointingEnabled/attribution(旧includeCoAuthoredByではない)等の推奨キーを正しく設定済みと判明し、明確なバグは検出されず前回(skill-effectiveness-audit)ほどの是正項目は無かった。判断が必要な2項目を人間へ確認: (1) permissions.denyリスト追加(security-gate.shと二重化) → 追加しない(現状維持、hooksが唯一の信頼できる強制機構という調査結果と整合するとの判断)。(2) カスタムsubagentへの`isolation: worktree` frontmatter導入(swarm-implementの既存worktree-alloc.shとの並存/置換) → 検討開始、ただしアーキテクチャ変更のため`/swarm-architect`招集が必要と判断し本ミッションでは未実装のまま人間へ引き継ぎ(blocked(design))。Agent Teamsの実在は公式専用ドキュメントの存在で再確認(前回のswarm-evolve改訂と整合、追加編集なし)。DeepResearchのverify段階でWebページ内のプロンプトインジェクション疑いの文言をシステムが検知・拒否(実害なし、ユーザーへ報告済み)。学び: 既に設定が十分成熟している対象への「改善リサーチ」ミッションは、無理に変更点を捻出せず「現状維持が正しい」という結論も正当な成果として扱うべき(前回のsuccessは5件の実装、今回のsuccessは2件の的確な判断)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-14 | skill-agent-quality-uplift: 「DeepResearchでSkill/Agentのコンテンツ品質を使いやすさ・性能の観点で改善」ミッション(Mission scale、3回連続の類似依頼) | 1                                                  | success | DeepResearchでAnthropic公式のAgent Skills執筆ガイダンス・Subagent設計ガイダンス・2026年の高性能agentテクニックを調査。最重要発見: SKILL.md本体は500行以内・詳細参照は1階層深い別ファイルへ、という公式ガイド(platform.claude.com/docs, code.claude.com/docs)に対し、7件のSKILL.mdが500行超過(python-testing 816〜rust-testing 502)と判明。人間に2点確認: (1) 7件全て分割 → 承認、(2) code-reviewer/security-audit/vald-reviewerのBash権限を読み取り専用に削るか → 現状維持(hooksが安全網・lint/test実行の実効性を優先)。7件をMaker(Sonnet)/Checker(Opus)ペアでworktree隔離しつつ並列上限3で分割実施(バッチ1: python-testing/cpp-patterns/python-patterns、バッチ2: golang-patterns/golang-testing/security-review、バッチ3: rust-testing)。Checkerは全件でコードブロック/チェックリスト項目をバイト単位diff・アンカーリンク解決・frontmatter不変・500行制限・1階層ルールの5観点を反証的に検証し全件承認(内容欠落ゼロ)。副次発見: agent frontmatterのeffort設定は既に全件正しく設定済み(CLAUDE.mdの記載と一致、追加作業不要)。学び: (1) Agent(sonnet)へのタスク委譲時、稀にauto modeクラシファイアがstage1判定に基づき一時的にツール呼び出しを拒否することがあり(transient、再試行で成功)、swarm-loopのFixerパターンとは別に単純リトライで解決する事例として記録。(2) 3回連続で「Claude関連のものを良くして」という類似依頼が来ており、AGENTS.md 2026-07-13の「2回目の学び機械化」トリガーに続き、今回で3回目相当。まだ機械化(swarm-loop Phase -1への組み込み)は未着手のまま — 次回同種の依頼が来た際は先送りせず`/swarm-evolve`での機械化を検討すべき水準に達している                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-14 | swarm-evolve: 自己改善ミッションの対象重複チェック機械化(3回連続予告された懸案の解消)                                                               | 2(Checker1回目は不合格、修正指示反映のv2で承認)    | success | 証拠: AGENTS.md軌跡に「曖昧なClaude Code改善依頼」が4件独立発生(一般レビュー/skill-effectiveness-audit/settings-modernization/skill-agent-quality-uplift)、うち3件で「次回は機械化を検討する」との予告が繰り返され未着手のまま先送りされ続けていた。Drafter初稿はPhase -1へのprose追記(段階2止まり)を提案しCheckerが不合格判定: (1)Phase -1は規模判定専用で角度選択の配置場所として誤り (2)Mission「人間不在の自律実行」原則と人間確認分岐が正面衝突 (3)§5が要求する機械化(hook/lint/test)ではなくprose明文化止まり (4)「重複しない角度」の判定基準が曖昧で運用不能。Drafterが修正版(v2)で対応: (1)配置をPhase 0 INIT(AGENTS.mdを読む箇所)へ移動 (2)人間確認はInteractiveのみに限定、Missionは停止せず@fix_plan.mdへ非ブロッキング記録しGATEで提示 (3)新規`self-improve-check.sh`(実シェルスクリプト、部分集合演算)+`self-improve-registry.tsv`(過去4ミッションの対象集合)を新設しmission-init.shに配線、真の段階3機械化を実現 (4)「新対象集合⊆過去ミッション対象集合」という具体的集合演算に判定基準を落とした。Checkerはbash実機でスクリプトを実行し動作を検証した上で承認(適用条件: レジストリ末尾改行・chmod +x、いずれも遵守済み)。人間承認後、claude/skills/swarm-loop/{SKILL.md, scripts/mission-init.sh, scripts/self-improve-check.sh(新規), self-improve-registry.tsv(新規)}に適用、テスト実行で3ミッションとのOVERLAP検知を確認。副次発見: 動作確認中に`claude/hooks/security-gate.sh`の`rm -rf /`パターンが終端アンカー`$`を欠いており、`rm -rf /tmp/anything`のような正当な絶対パス削除やBashコマンド文字列内に該当文字列が含まれるだけ(実行を伴わないecho/python3文字列リテラルでも)で誤ブロックされる広範な既知バグを発見(現時点で1回のみの発見のためswarm-evolveの2回反復基準未達、今回は機械化提案せず記録のみ・ユーザーへ直接報告)。学び: §5「学びの3段階モデル」に基づく機械化は、Checkerが「prose追加は段階2止まり」と明確に指摘したことで初めて段階3(決定論的スクリプト)まで到達した — Checkerの役割は「証拠の反復性」だけでなく「機械化の水準」も検査対象になり得ることを実証した事例 |
| 2026-07-14 | security-gate.sh: rm -rf / パターンの誤検知を人間の直接指示で即時修正(swarm-evolveの通常フロー経由ではない)                                         | 1                                                  | success | 直前のswarm-evolve作業中に発見した`rm -rf /`パターンの終端アンカー欠如バグ(正当な`rm -rf /tmp/foo`等を誤ブロック、実行を伴わない文字列リテラルでも誤ブロック)について、発見は1回のみでswarm-evolveの2回反復基準に達していなかったためユーザーへ報告のみに留めたところ、ユーザーから「すぐに直接修正してください」と明示指示があり即座に対応。`'rm -rf /'`→`'rm -rf /([[:space:]] | \* | $)'`(rm -fr も同様)へ変更し、/の直後が空白・ワイルドカード・行末のいずれかの場合のみマッチするよう修正。安全なコマンド(rm -rf /tmp/foo等)が誤検知されないこと、危険なコマンド(rm -rf /, rm -rf /\*, rm -rf / --no-preserve-root等)は引き続きブロックされることをPythonスクリプト経由で検証済み(検証コマンド自体に危険文字列を直接埋め込むとhookに誤ブロックされるため、Writeツールでファイル化してから実行する回避策を使用)。学び: 人間からの明示的な即時修正指示は、証拠反復数を問わずswarm-evolveの通常ゲートをバイパスして直接対応してよい(SWARM.md §5の2回反復ルールは自律判断時の抑制であり、人間の明示指示を妨げるものではない) |
| 2026-07-17 | swarm-tmpfile-session-scope: swarm系skillの一時ファイルを`/tmp/{session_id}/`配下へ統一(Interactive scale、人間の直接依頼) | 1 | success | 対象6箇所(swarm-architect/evolve/release-gate/implement/loopのSKILL.md本文・verify.sh)をgrepで全数確認し、実在するenv var`CLAUDE_CODE_SESSION_ID`(未設定時`manual`にフォールバック)を使い`/tmp/${CLAUDE_CODE_SESSION_ID:-manual}/swarm/`に統一。self-improve-check.shが過去3ミッション(いずれもskills-content対象)とのOVERLAPを検出したが、対象が「一時ファイルパス規約のみ」という狭いスコープで内容面レビューではないため続行と判断(差別化角度を@fix_plan.mdに記録)。影響ファイル数が「6以上でMission昇格」というswarm-loop自身の閾値に文字面では該当したが、設計判断は既に自己解決済み(session_id取得方法・永続化要否の2点のみ)で複数システム/自律実行を要さない機械的な文字列置換だったため、Interactiveのまま続行(Simplicity firstを優先、Mission機構の起動コストに見合わない)。swarm-evolveのdraft/verdict/却下保留分は次回`/swarm-evolve`実行時の再提示抑制のためセッションを跨ぐ必要があり例外的に`~~/.claude/session-data/swarm/evolve-proposals/`へ配置(ユーザーが最初の依頼で明示した「セッションを跨ぐ場合は.claude以下を許可」という例外条件に合致)。verify.sh実行で新パスが実際に機能することを確認(ログが実session_id配下に生成された)。副次発見: verify.shの`zsh -n`構文検査が`zsh/01-tmux.zsh.zwc`(コンパイル済みバイナリ)を誤って検査対象に含めFAILする既存バグを発見したが本ミッションと無関係のためOut of Scopeとして未修正のまま記録のみ(1回のみの発見でswarm-evolveの2回反復基準未達)。**設計上の論点**: ユーザーから「`@fix_plan.md`もソースディレクトリに置くべきでない一時ファイルではないか」という指摘があり、symlink化/完全移動/現状維持の3択を提示した結果「現状維持」を選択(複数repo/worktreeでの進行中ミッション検出をゼロコストで行うプロジェクトルート配置の設計上の理由が優先された)。学び: ファイル数閾値のような機械的なMission昇格基準は、設計不確実性が実質ゼロで単一セッション内完結する変更には過剰適用しない判断が有効だった(Auto Mode/Simplicity firstとの整合) |
| 2026-07-17 | swarm-mas-research: Multi-Agent/Agent Swarm/Agent Orchestratorの最新動向をリサーチしswarm系skill群の改善点を調査するミッション(Mission scale、人間の直接依頼) | 2(Checker1回目は差分1のみ不合格、修正指示反映のv2で承認) | success | deep-research workflow(108体・約534万トークン)で観点1(verifier独立性)・観点2(debate/self-refinement)に新規性が集中と判明、観点3(MAST)は既知の再確認のみ、観点4-7(Orchestrator設計/ベンダー公式/budget・retry定量知見/破滅的忘却機械化)は今回該当知見なし(不在の証明ではなく追加調査の余地ありとして記録)。新規知見はいずれも既存設計(§2決定論的ツール第一権威・intra-family限界・討論禁止、§8のMAV見送り)を裏付ける方向で、状態機械・並列数・試行回数・禁止事項などの挙動変更を要する発見はなし。swarm-evolveのDrafter(sonnet)/Checker(opus)機構を、2026-07-13の先例(DeepResearch起点のSWARM.md改訂)と同型の例外パスとして流用し、SWARM.md §2/§8へのdocs-only引用追加4件(BEI/CIG・Nine Judges Two Effective Votes・GroundEval・debate3要因分解・identity駆動sycophancy・4段階検証権威階層・金融MAS創発バイアス)を起案・検証・人間承認・適用。Checkerが差分1(確定バケット)に証拠にない実験内訳の捏造(「5モデル系列×4ベンチマーク」、証拠は「20組中18組」としか述べていない)を検出し不合格→削除して再検証で承認、という機械的な逐語チェックの実例を得た。self-improve-check.shは対象集合{skills-content, multi-agent-mechanism}が過去4ミッションいずれの対象集合の部分集合でもないと判定しNO_OVERLAP(初のこの組み合わせ)。学び: (1) 外部一次資料を要約・再構成する際、Drafterは算術的に整合する尤もらしい実験内訳を無根拠に補完しがちである — Checkerは「証拠一式とnew_stringの逐語比較」で機械的にこれを検出可能であり、swarm-evolveのCheckerの役割に「引用の逐語的忠実性(捏造された数値・実験設計の混入)検査」を加えるべき実例が2件目(前回はconfidence表記の混同)となった。(2) 「Claude関連のものを良くして」系の曖昧な依頼とは異なり、本ミッションは「特定分野(Multi-Agent/Swarm/Orchestrator)を外部リサーチしてから内部と突き合わせる」という具体的な起点を持ち、self-improve-registry.tsvのmulti-agent-mechanismタグが単独ヒットしたのはsettings-modernization(2026-07-14)のみで、今回のskills-content併記は新規角度として機械的に確認された。(3) 今回「新規知見が既存設計を裏付けるのみで挙動変更なし」という結論は、2026-07-14のsettings-modernizationと同型の「現状維持が正しいという結論も正当な成果」パターンの3件目に相当する。
| 2026-07-17 | nested-swarm-loop-depth-budget-guard: 案A(Agentによる再帰的/swarm-loop自己起動を深さ1・共有予算上限つきで許可)の実装(Interactive scale、人間の直接依頼) | 2(attempt1はGREEN後にcode-reviewerが並行実行時のレースコンディションを実測検出しCheckerの承認と食い違い、hookではなく実行証拠を優先して差し戻し。attempt2で修正・再承認) | success | `/swarm-architect`承認済み提案書(案A)に基づきTest Maker(RED)→Maker(GREEN)→Checker(opus)+code-reviewer並行検証で実装。`mission-init.sh`に`depth`引数(深さ1超で`REFUSE`)、`budget-guard.sh`に`--mission=<slug>`共有予算カウンタ、`swarm-implement/SKILL.md`のFixerセクションへネスト提案条件(depth1限定・Interactive限定・worktree必須・GATE到達不可でblocked/ESCALATEのみ復帰)を追記。**当初「Aのみ採用してください」という指示が、提案書自身の推奨(「見送り、Fixerで解決できない具体的事例が2件蓄積するまで待つ」)を上書きして今すぐ実装する意味か、方向性の確定のみを意味するか曖昧で、auto modeクラシファイアがSelf-Modification理由でMaker起動を一度ブロックした。AskUserQuestionで「今すぐ実装する」と明示確認を取ってから再開**。attempt1はCheckerが承認したが、並行スポーンしたcode-reviewerが40並列呼び出しの実測(期待40・実測6)でレースコンディション(共有カウンタのread-modify-writeにロックなし)を検出。SWARM.md §2の「hook結果を優先」はChecker対hookの規定だが、Checker対並行レビューの食い違いでは実行証拠(実測再現)を伴う指摘を優先すべきと判断しattempt2へ差し戻した。attempt2で`flock`によるアトミック化・`--mission-max`数値検証・task予算超過時も共有カウンタを増分する順序変更・予約接頭辞`_mission-total-`衝突ガードを追加、20並列/40並列/50並列の回帰テストで再現確認。Checker再検証・code-reviewer再検証とも承認。学び: (1) 高コスト層(swarm-architect)への招集は`disable-model-invocation: true`によりSkillツール経由の自然文承認では起動できず、ユーザーがスラッシュコマンドを直接タイプする必要がある(意図的な摩擦、今回はユーザーに案内して`/swarm-architect`を直接実行してもらった)。(2) 「案を採用する」という指示と「今すぐ実装する」という指示は別物であり、提案書が「採用するが実装は見送る」という二段構えの推奨をしている場合、ユーザーの短い承認文だけでは自動モードの安全策(Self-Modification分類)が妥当な理由でブロックすることがある — 曖昧なら実装前にAskUserQuestionで解消するのが正しい対応だった。(3) Checkerは並行実行下の挙動までは検証しておらず、code-reviewer(並行スポーンした追加レンズ)が実行証拠つきで発見した — SWARM.md §2の「Checkerとcode-reviewer等の並行レビューは代替ではなく追加のレンズ」という設計が実際に機能した実例。
| 2026-07-17 | all-skills-phrasing-audit: ~/.claude/skills配下全28 SKILL.mdの不自然な構造・不要な文言・不自然な日本語文章の監査(Mission scale、人間の直接依頼) | 1 | success | Haiku 9シャード探索(全28 SKILL.md全文読解)→secretary(sonnet)集約で4件検出。一次ファイルで裏取りした結果、3件は誤検知と判明: (1)dig/SKILL.mdの`swarm-loop`参照を「存在しないskill」と誤判定(実在する現行skillであり本ミッション中も多用)、(2)(3)swarm-loop/SKILL.md 224-225行の日本語指摘2件はいずれも引用自体が実ファイル内容と一致しない幻覚(「セッション を跨いで」という余分なスペースは実在せず、「〜であること」という体言止めは規定文の標準的な表現で破綻していない)。severity=infoでnoise扱いされ一度は除外されたcpp-patterns言語混在指摘のみ一次ファイル確認で実在の問題と確認: SOLIDテーブルのみ日本語で他は全編英語という不整合を発見・英語に統一。追加で自分で行った機械的裏取り(reference.md実在確認7件・skill間相互参照解決確認・allowed-tools内tool名確認)でswarm-architect/SKILL.mdの`SendUserFile`(存在しないtool、本セッション中の実使用でToolSearch経由でも見つからず失敗した経緯あり)を追加検出し、会話内提示への修正に置き換え。自作の見出し階層ジャンプ検出awkスクリプトもコードフェンス内の`#`コメント(例: dockerfile例中の`# After verification:`)を見出しと誤認する誤検知を埋め込んでおり、裏取りの結果全件破棄。self-improve-check.shは過去4ミッション(いずれもskills-content関連)とのOVERLAPを検出したが、対象が「日本語文章の自然さ・構造の一貫性」という言語表現レベルの品質(behavioral変更なし)に限定され過去の機能的正しさ監査群とは角度が異なるため続行。学び: (1) 2026-07-13 skill-effectiveness-audit(61件中55件誤検知)と同型のHaiku誤検知パターンが「設定監査」だけでなく「日本語文章品質監査」でも再現し、Haiku群の低信頼性はドメインを問わない普遍的傾向であると確認された。(2) 「一次ファイルで裏取りする」原則は他エージェント(Haiku)由来の指摘だけでなく自分自身が書いた機械的チェックスクリプトの結果にも同様に適用すべき — 今回、検証用awkスクリプト自身のバグ(コードフェンス非考慮)を自ら発見・破棄できたのは、Haiku指摘と同じ厳格さで自己検証したため。(3) `~~/.claude/skills`のようなsymlink経由のパスは`find`再帰時に0件を返す環境固有の癖があり(readlink -fで解決した実パス経由なら正常動作)、今後この種の機械的全件チェックでは実パスを使うこと。
| 2026-07-18 | swarm-explore-effort-routing: Haiku探索エージェントのeffort(low/medium)をモデルと独立にルーティングし秘書への情報欠落を減らす(Interactive scale、人間の直接依頼) | 1 | success | 発端は「Haikuが収集した情報をまとめて秘書に伝える段階で欠落することがある」という人間の所感。既存は全シャード一律`model:haiku, effort:low`固定だった。AskUserQuestionで基準を確認し「シャード規模による静的判定+秘書判定による差分再探索の併用」「秘書側のeffortは対象外」を選択。実装: (1)`shard-targets.sh`をround-robin均等分割からLPT法(重い順に最小合計シャードへ割当)へ変更し出力を`[{dirs, weight}]`に(weight=シャード内対象ファイル数)。docker対象は元々ファイル単位でweight固定1のため意味を変えないよう`-printf '%h\n'`を追加せずそのまま`uniq -c`をかけた。(2)`swarm-explore/SKILL.md`のWorkflowに`WEIGHT_THRESHOLD=15`超で`effort:medium`から開始する静的ルーティング、秘書が`low_quality_shards`(曖昧・矛盾・件数僅少と判定したシャード番号)を返したらそのシャードのみ`effort`を1段階昇格(low→medium→high)して差分再探索しマージする2段階pipelineを追加。(3)`swarm-secretary/SKILL.md`に責務5「品質評価」を追加し`{report, low_quality_shards}`の構造化出力契約を明記。(4)`SWARM.md`§1ルーティング規則にeffort軸の原則を追記。self-improve-check.shは過去4ミッション(いずれもskills-content)とのOVERLAPを検出したが「モデルに加えeffortという新しいルーティング軸を追加する」という技術的に異なる角度のため続行と判断。shard-targets.shはvald(go/rust/docker各lang・エッジケース)とdotfilesで実行し重み付き分割を確認、Workflowスクリプトは`node --check`で構文検証後、`agent`/`parallel`等をモックしたNode.jsシミュレーションでeffortルーティング分岐・再探索あり/なし両ケースの実行結果を検証した(実際のWorkflow実行はしていない)。学び: (1)「モデルルーティングだけでなくeffortもルーティングする」という発想は既存のSWARM.md§1表(層→モデル)には無かった軸であり、コスト構造(Haikuは安いがeffort段階でトークン消費が変わる)を保つには「全体の底上げ」ではなく「規模ベースの静的振り分け+実際に低品質と判定された分だけの限定的動的昇格」という二段構えが必要だった。(2)既存コードの一見冗長に見える実装差異(list_dockerだけ`-printf '%h\n'`が無い)は「Dockerfileはファイル単位、go/rustはディレクトリ単位」という意図的な設計であり、変更時に安易に統一するとターゲットの意味自体が変わってしまう典型例だった。
| 2026-07-18 | swarm-memory-bridge: swarm-loop各層(Haiku探索群/秘書/Maker/Checker)の知見と人間対話の知見をauto-memoryへ集約するブリッジ機構の構築(Interactive scale、人間の直接依頼、「Webの本日時点の最新Claude Code利用を調査し最適な方法を構築せよ」という指示) | 1(code-reviewerが1回で3件の実質的指摘を検出、即修正して反映) | success | Web調査(公式ドキュメント`code.claude.com/docs/en/sub-agents`・`/hooks`を`WebFetch`で裏取り)で、①subagent単位のcross-session知識蓄積を可能にする`memory` frontmatterフィールド(スコープ`user`/`project`/`local`)が既に公式機能として存在すること、②サブエージェントはメインのauto-memory(`~/.claude/memory/`)を自動ロードしないこと(CLAUDE.mdのみロード)、③`SubagentStop`hookは存在するが詳細ペイロード仕様は非公開ドキュメント部分で未確認、の3点を確認。設計はAskUserQuestion 4問(全体レベル/対象subagent/Fixer方針/新skill承認方式)で人間と合意し、`debugger`は「Fixerは失敗履歴を持たない汚染されていない視点で再診断する」既存原則(SWARM.md §5)を優先しmemory対象から除外、`code-reviewer`/`vald-reviewer`に`memory`フィールドを付与、新設skill`swarm-memory-sync`(`swarm-evolve`と対、AGENTS.md/@fix_plan.mdの学びのうち一般化可能なものをauto-memoryへ蒸留、人間承認不要=知識の記録であり行動規範の変更ではないため)を`swarm-loop`Phase 2 PLAN・Phase 5 GATEに配線した。実装後`code-reviewer`によるレビューで重大な見落としを検出: `~/.claude/agent-memory/`を`find`すると、`go-expert`/`rust-expert`/`security-audit`/`proto-expert`/`vald-reviewer`には既に2026-05-27付の蓄積済み知見(`vald_laws.md`等)が存在し、`go-expert`/`rust-expert`は`memory: user`スコープで運用されていたが`vald-reviewer`だけ現行agentファイルから`memory`フィールド自体が欠落していた(過去に試験導入されたが削除された、またはCLAUDE.mdのSubagentsテーブル・SWARM.mdのどちらにも一度も記載がないまま運用されていたと推測)。当初`vald-reviewer`に`memory: project`を付与したが、これは`<project-root>/.claude/agent-memory/`(vald repo内)を指すため既存の`~/.claude/agent-memory/vald-reviewer/`資産が孤立するバグだった→`memory: user`に修正し既存資産をそのまま継続利用可能にした(`go-expert`/`rust-expert`と同じパターンに統一)。また`code-reviewer`は「`swarm-memory-sync`の重複・肥大化防止がLLMの自己判断のみに依存しており、SWARM.md §2/§6の『決定論的ツールを第一権威とする』既存原則と矛盾する」と指摘→`memory-guard.sh`(MEMORY.mdサイズ警告+キーワードgrepによる重複候補提示)を新設しSKILL.md手順に必須ステップとして組み込んだ。Phase 2 PLANからの内部呼び出しトリガーが`swarm-memory-sync/SKILL.md`自体に記載されていないドキュメント間の非対称も検出・修正。学び: (1) subagent memory機構は本ミッション以前から一部agentで運用されていたが、CLAUDE.mdのSubagentsテーブルにもSWARM.mdにも一度も記載がなく、公式ドキュメントで機構の存在を確認しただけでは気づけず、`~/.claude/agent-memory/`を実際に`find`して初めて判明した——「公式ドキュメントでの機構確認」と「既存のファイルシステム状態の裏取り」は独立に両方必要という実例。次回スコープ外として`proto-expert.md`(既存資産はあるが`memory`フィールド欠落)を発見済み・未修正のまま記録(1回のみの発見、機械化見送り)。(2) 名前付きAgent(非fork、`subagent_type: claude-code-guide`)への調査委譲で、完了後に`idle_notification`のみが3回連続で返り実質的な回答本文を得られない事象が発生→teammateを停止し`WebSearch`/`WebFetch`を直接実行して解決。1回のみの事象のため機械化は見送るが、次回同型の停滞が起きたら「待ち続けず直接ツールに切替える」判断を優先する。(3) `swarm-evolve`(Skill行動規範の進化)と`swarm-memory-sync`(ドメイン知識の蒸留)の役割分担は、既存のAGENTS.md蓄積(2026-07-13〜17分)を眺めれば「一般化可能なのに埋もれている学び」(Haiku群の低信頼性パターン等)が既に大量にあることが分かる——次回`/swarm-memory-sync`の遡及処理でこれらの移行を行う見込み(本ミッションでは未実施)。
| 2026-07-18 | memory-driven-agent-skill-dev: `~/.claude/memory/`(auto-memory)全40件を一次資料として読み込み、頻出しているのに専用Agent/Skillが無いパターンを検出して新規作成(Interactive scale、人間の直接依頼「過去のClaude利用方法をMemoryから探索し、必要なAgents及びSkillを開発せよ」) | 1(Opus Checkerが1回で3/3 PASS判定、軽微な指摘2件を即修正) | success | self-improve-check.shで過去4件(skill-effectiveness-audit等)とOVERLAP検出したが、過去は全て「既存Agent/Skillの中身改善」または「メタ機構構築」で、本ミッションは「Memory内容(過去の実利用履歴)からのギャップ検出→新規作成」という逆方向のため続行と判断。全40件精読の結果、ArcFlare(自作ANNベクトル検索エンジン)+NGTAQ再設計に関するMemoryが20件超を占め、SIMD(AVX2/AVX-512)距離カーネル最適化・ann-benchmarksでのrecall@k/QPS Paretoフロンティア分析・量子化(SQ2/SQ4/SQ8/RaBitQ)の数学的等価性検証が既存`perf-analyzer`(汎用pprof/perf)や`benchmark`skill(Web Vitals/API/build)に一切カバーされていないと確認。副次的にvald/dotfiles横断のCI根本原因分析(5件、libomp/BLAS/rustfmt component/reusable workflow権限等の層状原因)も発見。AskUserQuestionでユーザーに(1)ANNギャップはAgent+Skill両方(proto-expert+protobuf-patternsと同型)、(2)CIギャップは`debugger`/`github-actions-patterns`への追加でなく専用Agent新設、(3)命名は提案通り(ann-perf-engineer/ann-benchmark-patterns/ci-investigator)、の3点を確認して実装。Opus Checker(独立コンテキスト、fresh agent)に全技術的主張をMemory原本と逐語照合させた結果3/3 PASS、指摘は2件とも低severity: recall安定性の記載値(±0.0001-0.002)のうち0.002が別文脈(Pareto gap-increment閾値)からの誤流用だった点、gap-fill統合判定を出典に無い3段階表として捏造していた点(出典は「非ドミナント3件以上 かつ 各+0.002 recall gap」という単純ルール)——即修正して反映。学び: (1) `~/.claude/memory/`は40ファイル中19件のみがMEMORY.mdにインデックスされており、21件(ArcFlare関連の詳細ログが大半)が孤立していた——MEMORY.md索引の網羅性を前提にせず`ls`で実ファイル数を確認すべき(次回`/swarm-memory-sync`遡及処理の対象候補として記録、本ミッションでは未着手)。(2) `~/.claude/agents`・`~/.claude/skills`はdotfilesリポジトリの`claude/agents`・`claude/skills`へのsymlinkであり、新規ファイル作成が自動的にgit管理下に入る——事前に`readlink -f`で確認したため二重管理を回避できた。(3) Checker(Opus、named agent)への完了確認は`SendMessage`で明示的に催促した際に即座に本文が返り、[[feedback_named_agent_stall]]の停滞パターンは再現しなかった。
| 2026-07-18 | agent-self-evolution-maintenance: 「AgentsおよびSkillsを各Agentが自己進化・自己メンテナンスさせる」仕組みの構築(Interactive scale、人間の直接依頼) | 1(Opus Checkerが1回で8/8 PASS判定、軽微指摘3件中2件を即反映) | success | self-improve-check.shでswarm-memory-bridge(CLAUDE.md,agents-content,skills-content,multi-agent-mechanism)とOVERLAP検出。差別化: swarm-memory-bridgeは知見蒸留機構自体の新設だったが、本ミッションは既存2機構(①agent memoryフロントマター=自己メンテナンス、②swarm-evolve=自己進化)の**カバレッジ拡張・自動化強化**が主眼で新規メタ機構は作らない、と整理して続行。設計はAskUserQuestion計5問(自己進化の自動化範囲/自己メンテナンス拡張範囲/Skill自己メモリ機構の要否/自動化の深さ/対象範囲)で人間と段階的に合意。特に「swarm-evolveの人間承認省略不可原則」は過去mission(2026-07-14)でCheckerが一度明確に拒否した経緯のある意図的な安全設計であり、本ミッションでは「証拠収集→Drafter→Checkerまで自動化、適用(Step5)のみ人間承認」という最も保守的な選択肢が選ばれた。実装: (1) `swarm-evolve`の`disable-model-invocation`を`true→false`に緩和(swarm-memory-syncと同じ理由、Step4/5の人間承認必須性は本文に明記し不変)、(2) `swarm-loop` Phase 5 GATEに新規ステップ2としてswarm-evolve自動呼出を配線しQuickモードは除外、(3) `proto-expert.md`の既知バグ(既存`~/.claude/agent-memory/proto-expert/`資産があるのに`memory`フィールド欠落、2026-07-18 swarm-memory-bridgeで発見済み未修正だったもの)を修正、(4) `perf-analyzer.md`に`memory:user`+Memory Protocol節を新規追加、(5) `~/.claude/skill-memory/<name>/MEMORY.md`という新規convention(Skillには公式`memory`フロントマターが無いため自前設計)をswarm-explore/swarm-implement/swarm-architect/swarm-release-gateの4 SKILL.mdへ追加。当初承認された5skill対象のうちswarm-secretaryは、SKILL.md本文が明示する「入力レポート以外のファイル読み込み禁止・新規調査禁止」という純粋関数的設計と直接矛盾するため、実装中の一次ファイル確認で自ら発見し除外(人間には結果を透明に報告、事前承認は取り直さず)。Opus Checker(独立コンテキスト)が8ファイル全件PASSと判定、「人間承認省略不可」原則と「verify.shは記憶で代替不可」原則がいずれも弱められていないことを最優先項目として確認。軽微指摘3件のうち2件(swarm-explore/swarm-release-gateのallowed-toolsにWrite追加、proto-expertへのMemory Protocol節追加)を反映、残り1件(Step4ラベルの用語ドリフト)はCheckerも「変更不要」と判定し見送り。学び: (1) 影響ファイル数が9件でPhase -1の「6件以上でMission昇格」基準に技術的に抵触したが、全設計判断が本Interactiveセッション内でリアルタイムに人間確認済み(Mission scaleが代替する「人間不在時の判断」を既に上回る)かつ単一エージェントの逐次編集(worktree必須条件に非該当)だったため、Mission再起動はせず透明性の記録のみでInteractiveのまま続行した——スケール閾値は昇格シグナルであり機械的な強制再起動ではないという運用解釈の実例。(2) 「Skill自体に自己メンテナンス機構を作る」という一見一様に適用できそうな要求も、対象ごとの既存設計原則(swarm-secretaryの純粋関数性、swarm-release-gateの「記憶は検証の代替にならない」原則)を個別に読み込んで初めて適用可否が分かる——複数ファイルへの同一パターン適用でも一次ファイル確認を省略してはならない実例。(3) 直前の[[memory-driven-agent-skill-dev]]ミッションでself-improve-registry.tsvへの登録を1回失念しており、今回のGATEで気づいて追記した——registry追記はmission-init.shが自動化していない手動ブックキーピング項目であることを再確認。
| 2026-07-18 | agent-skill-refinement-pass: 「Agent/Skillを全体的に最適化・洗練させる」4回目の監査依頼(Interactive scale、人間の直接依頼) | 1(Opus Checkerが1回で3/3 PASS判定) | success | self-improve-check.shで5件(skill-effectiveness-audit/skill-agent-quality-uplift/swarm-memory-bridge/memory-driven-agent-skill-dev/agent-self-evolution-maintenance)とOVERLAP検出。過去3回の全corpus監査は別々のレンズ(①設定/hook配線の正しさ ②500行超分割等の使いやすさ ③日本語フレージング)だったため、AskUserQuestionで人間に4回目のレンズを確認したところ「全corpusをもう一度監査」+「クロスリファレンス・整合性監査」(4番目の新レンズ)を明示選択。実行方式もAskUserQuestionで確認し、機械的な存在確認はHaiku群より決定論的ツール(grep/bash)が優越する(SWARM.md §2)としてswarm-explore(Haiku dispatch)を使わずセッション内で直接検査(Mission昇格はしない判断)。発見: (1) `cpp-patterns/SKILL.md`のfrontmatter`name`が`cpp-coding-standards`でディレクトリ名`cpp-patterns`と不一致(2026-07-17 all-skills-phrasing-auditで既に指摘されていたが未修正のまま放置されていた既知バグ、invocation解決に影響しうるため優先度高と判断し修正)。(2) 自作grepスクリプトが「CLAUDE.md Skillsテーブルにswarm-explore/swarm-implementが無い」という誤検知を出したが、実ファイルを直接読んで「(swarm-loop内部/直接)」という`/command`以外のtrigger表記だっただけと判明し、編集前に自己訂正(誤検知のまま反映しなかった)。(3) `self-improve-registry.tsv`への登録漏れが本日だけで2回(all-skills-phrasing-audit/memory-driven-agent-skill-dev)発生していたためSWARM.md §5「2回目の学びの機械化」原則に従い`self-improve-register.sh`(冪等・日付検証つき)を新設し`swarm-loop`Phase 5 GATE Step 4から自動呼出するよう配線(ユーザーがAskUserQuestionで明示的にスコープへ追加を承認)。Opus Checker(独立コンテキスト)が3変更全てPASSと判定、誤検知の自己訂正も正しいと確認、スクリプトのシェルインジェクション・冪等性・レジストリ状態(テストエントリ残存無し)のスポットチェックでも新規発見なし。学び: (1) 自作の検証スクリプト(grepベースのcross-reference検査)自身が、trigger列の表記ゆれ(`/command`形式 vs `(内部呼出)`形式)を考慮していなかったため誤検知を出した——2026-07-17のall-skills-phrasing-auditで学んだ「自作チェックスクリプト自身にもHaiku指摘と同じ厳格さで裏取りする」教訓がここでも有効だった実例。(2) 「Agent/Skillを良くして」という曖昧な依頼が5回目相当に達しても、機械的な重複検出(self-improve-check.sh)と人間へのレンズ確認(AskUserQuestion)を組み合わせれば、収穫逓減を避けつつ正当な差別化(今回はクロスリファレンス監査という手つかずの角度)を都度発見できる——機械化された重複チェックは「もうやるな」でなく「同じことをするな」を強制する仕組みとして機能している。
