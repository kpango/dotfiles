# fable スポット判断層 + 試行予算の定数の単一ソース (SWARM.md §1/§3 の記載値と対応)。
# budget-guard.sh / swarm-fable-gate.sh / swarm-write-scope-gate.sh / swarm-parallel-gate.sh /
# parallel-gate.sh / loop-status.sh が source する。
# 読み込み側は本ファイル欠落時に同値のフォールバック既定値を持つ (fail-safe)。
# 値を変更したら SWARM.md §1/§3 の記載も更新すること。
# テスト・実験用に FABLE_BUDGET_CONF 環境変数で別ファイルを指せる。
FABLE_TASK_MAX=1
FABLE_MISSION_MAX=2
FABLE_GRANT_TTL_SECONDS=600
FABLE_SPOT_LOG_MAX_LINES=1000
FABLE_SPOT_LOG_KEEP_LINES=500
BUDGET_TASK_MAX_DEFAULT=5
BUDGET_MISSION_MAX_DEFAULT=20
WRITE_SCOPE_GRANT_TTL_SECONDS=300
PARALLEL_GATE_TTL_SECONDS=1800
# SWARM.md §1「実装層 Maker/Checker の並列実行は独立タスクにつき最大3並列まで」の実数値。
# 2026-09-04以前はswarm-parallel-gate.sh(強制)とparallel-gate.sh --status(表示)の2箇所へ
# 直書きされ、値の食い違いリスクがあった(claude-hooks-full-agent-consolidationミッションで発見・
# 集約)。
PARALLEL_GATE_MAX_SLOTS=3
