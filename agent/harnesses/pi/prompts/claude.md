---
name: claude
description: Delegate a prompt or task directly to Claude Code CLI with full permissions and streaming feedback.
---

# Delegate to Claude Code

Execute via Claude Code CLI bridge:

**Prompt:**
{{input}}

Run using the `claude_code` extension tool or slash command `/claude`.
