---
name: codex
description: Delegate a prompt or task directly to OpenAI Codex CLI.
---

# Delegate to Codex CLI

Execute via OpenAI Codex CLI bridge:

**Prompt:**
{{input}}

Run using the `codex` extension tool or slash command `/codex`.
