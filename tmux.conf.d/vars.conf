# ── Runtime variables ─────────────────────────────────────────────────────────
# Non-colour constants sourced after colors.conf, before options.conf/status.conf.
# Reference via #{@var_name} in format strings and #(...) command strings.

# ── Binary / script paths ──────────────────────────────────────────────────────
set -g @bin_pane_info "$HOME/.zcache/tmux-pane-info"
set -g @bin_disk_info "$HOME/.tmux.conf.d/disk-info"
